Задание 1.
    pass

Задание 2.
    pass

Задание 3.
    1) Применен цикл while (инкремент индекса вручную)
    2) Проверяем является ли i-тый элемент числом методом isdigit() предварительно очистив от знаков, если они есть
    3) Метод zfill(width) дополняет строку слева нулями до указаной длины width, включая в длину знаки + или -
    4) Обособляем кавычками числа из списка
    5) формируем строку

Задание 4.
    Проход по элементам списка, разделение элемента методом split(), берем последний элемент, применяем метод title()
Задание 5.
    А.
        1) Дан список цен товаров
        2) В цикле for проходим по списку, функцией int() отсекаем дробную часть, вычисляем остаток (копейки), вывод на экран
    B.
        1) метод списка sort() без параметров сортирует список по возрастанию не создавая новый список
    C.
        1) функция sorted(list) создает новый отсортированный список. Параметр reverse=True определяет порядок
           сортировки: False(по умолчанию) - по возрастанию, True - по убыванию
    D.
        1) берем список отсортированный по возрастанию и применяем срез с -5 элемента до конца списка.